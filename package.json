{
  "name": "go-gestione-officina",
  "version": "0.1.0",
  "private": true,
  "description": "Gestionale web per officina meccanica, predisposto per Render e PostgreSQL.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "admin:create": "node scripts/create-admin.js",
    "check": "node --check src/server.js && node --check src/migrate.js && node --check scripts/create-admin.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "connect-pg-simple": "^10.0.0",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "pdfkit": "^0.17.1",
    "pg": "^8.16.3"
  }
}
