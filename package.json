{
  "name": "go-gestione-officina",
  "version": "0.2.0",
  "private": true,
  "description": "Gestionale web per officina meccanica, predisposto per Render e PostgreSQL.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "admin:create": "node scripts/create-admin.js",
    "superuser:create": "node scripts/create-superuser.js",
    "check": "node --check src/server.js && node --check src/migrate.js && node --check src/timezone.js && node --check scripts/create-admin.js && node --check scripts/create-superuser.js && node --check test/database.test.js && node --check test/timezone.test.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^18.0.4",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "multer": "^2.4.0",
    "pdfkit": "^0.17.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
